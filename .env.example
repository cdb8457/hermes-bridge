# Hermes Bridge — environment variables
# Copy to .env and adjust as needed

# Base URL for the Hermes backend (used by Vite dev proxy)
VITE_API_BASE_URL=http://localhost:8642

# App display name
VITE_APP_NAME=Hermes Bridge
